<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.davidpopayan.sena.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Controlador.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Videojuegos.play" />
        <activity android:name=".Animacion.Caja" />
        <activity android:name=".Videojuegos.infoplay" />
        <activity android:name=".Animacion.Infocaja" />
        <activity android:name=".Especializacion.especializacion" />
        <activity android:name=".Especializacion.infoespeci" />
        <activity android:name=".Login.LoginApp"></activity>
    </application>

</manifest>